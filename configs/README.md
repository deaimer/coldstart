# Evaluation Configurations

Store reviewed, version-controlled run configurations here. Configurations should pin the dataset version, task selection, agent, model, trial count, timeouts, resource policy, and network policy without containing API keys.
