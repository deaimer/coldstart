# Public Sample Tasks

This directory contains educational ColdStart tasks that demonstrate the format and methodology. Public sample tasks should not be treated as secure held-out evaluations once their environments, tests, or solutions are broadly available.

Create the first sample with:

```bash
uv run coldctl task init artifact-vault-recovery
```
