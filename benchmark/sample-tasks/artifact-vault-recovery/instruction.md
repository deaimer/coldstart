Artifact Vault at `http://127.0.0.1:8080` reports healthy, but legacy artifacts return storage errors, newly uploaded artifacts can disappear after service restarts, and concurrent retries using one idempotency key sometimes leave extra records. Diagnose and repair the Go service, PostgreSQL state, migration, and configuration under `/app`. Preserve the seeded artifact IDs and bytes; do not reset or replace the database. The repair must make legacy and new downloads work, make concurrent idempotent uploads converge on exactly one artifact, and survive a full API/worker restart. Readiness must reflect both database and storage availability.

Use `/app/README.md` for the local API and operator commands. When finished, create `/app/coldstart-report.json` containing `diagnosis`, `changes`, `tests_performed`, `remaining_risks`, and `evidence_files`; each value except `evidence_files` must be meaningful text, and `evidence_files` must be a non-empty list of paths. Leave the repaired services running.
